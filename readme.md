1️⃣ What is the difference between var, let, and const?

Answer :: var is old js variable now almost not use it .let is latest variable and can be change variable value another side const variable can't change value;



2️⃣ What is the spread operator (...)?

Answer :: spread oparator is expand an array and find array propertise too easy;


3️⃣ What is the difference between map(), filter(), and forEach()?

Answer :: map() is change an array and output is an array .filter() is use when we have some propertise is separate in main array; another side forEach() is just looping array and after display items in html;


4️⃣ What is an arrow function?

Answer :: arrow function is shorter version for function . less typing and latest vertion for es6.

5️⃣ What are template literals?

Answer :: `` its very powerful inside the all propertise are displayd as simple as posible; 